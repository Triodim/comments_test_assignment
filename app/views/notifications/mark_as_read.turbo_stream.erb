<%# Update the notification row styling %>
<%= turbo_stream.replace "notification-#{@notification.id}" do %>
  <%= render(NotificationRowComponent.new(notification: @notification)) %>
<% end %>

<%# Update bell badge count %>
<%= turbo_stream.replace "notification_badge" do %>
  <%= render(NotificationBadgeComponent.new(current_user: current_user)) %>
<% end %>
